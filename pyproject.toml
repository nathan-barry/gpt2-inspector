[project]
name = "gpt2-inspector"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fire==0.5.0",
    "numpy==1.24.1",
    "regex==2017.4.5",
    "requests==2.27.1",
    "tensorflow==2.11.0 ; platform_machine != 'arm64' or sys_platform != 'darwin'",
    "tensorflow-macos==2.11.0 ; platform_machine == 'arm64' and sys_platform == 'darwin'",
    "tqdm==4.64.0",
]
